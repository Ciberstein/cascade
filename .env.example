POSTGRES_PASSWORD=changeme
# No login: by default it only listens on this machine. Setting 0.0.0.0 opens
# Cascade to the whole local network, where anyone could queue downloads and
# change the engine limits.
BIND_ADDRESS=127.0.0.1
