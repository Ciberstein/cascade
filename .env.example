POSTGRES_PASSWORD=changeme
# Any long random string; changing it invalidates existing sessions.
JWT_SECRET=changeme-to-a-long-random-string
# The single account, created on first login.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme
# Dónde caen las descargas, en el host. Cada paquete crea su subcarpeta.
DOWNLOAD_DIR=./downloads
